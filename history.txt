#V2
select timestamp from `FFFFFFFFFFFF:Screen` where record::id(id) = '211f0090-89a1-4a01-890f-4ced13873a1c';
info for db;
select * from `FFFFFFFFFFFF:Screen:Ocr`
info for db;
delete `asdf:asdf`;
info for db;
delete  `asdf:asdf`;
select count(timestamp) from `FFFFFFFFFFFF:Screen:Ocr`;
select count() from `FFFFFFFFFFFF:Screen:Ocr`;
select count() from `FFFFFFFFFFFF:Screen`;
select count() from `FFFFFFFFFFFF:Screen:OCR`;
select count() from `FFFFFFFFFFFF:Screen:OCR` group all;
select count() from `FFFFFFFFFFFF:Screen:Ocr` group all;
select count() from `FFFFFFFFFFFF:Screen` group all;
select count() from `FFFFFFFFFFFF:Screen:Ocr` group all;
select count() from `FFFFFFFFFFFF:Screen` group all;
select count() from `FFFFFFFFFFFF:Screen:Ocr` group all;
select timestamp from `FFFFFFFFFFFF:Screen`
select text from `FFFFFFFFFFFF:Screen:Ocr` limit 3
select text from `FFFFFFFFFFFF:Screen:Ocr` limit 1
clear
select * from `FFFFFFFFFFFF:Screen:Ocr` limit 1
select count() from `FFFFFFFFFFFF:Screen`;
select count() from `FFFFFFFFFFFF:Screen` group all;
select count() from `FFFFFFFFFFFF:Screen:ocr` group all;
select count() from `FFFFFFFFFFFF:Screen:Ocr` group all;
select count() from `FFFFFFFFFFFF:Screen` group all;
info for db;
select uuid from `FFFFFFFFFFFF:Screen`
select record::id(id) from `FFFFFFFFFFFF:Screen`
select count() from `FFFFFFFFFFFF:Screen` group all;
select count() from `FFFFFFFFFFFF:Screen:Ocr` group all;
select text from `FFFFFFFFFF:Screen:Ocr`;
info for db
select text from `FFFFFFFFFFFF:Screen:Ocr`;
info for db;
SELECT id, text\nFROM `FFFFFFFFFFFF:Screen:Ocr`\nWHERE string::contains(text, "Jacen Wong") 
SELECT id\nFROM `FFFFFFFFFFFF:Screen:Ocr`\nWHERE string::contains(text, "Jacen Wong") 
DEFINE ANALYZER ocr_ascii_lower\n  TOKENIZERS class\n  FILTERS ascii, lowercase;\n
info for db;
insert into ocr_copy (select * from `FFFFFFFFFFFF:Screen:Ocr`);
info for db
DEFINE INDEX ocr_text_ft\n  ON TABLE ocr_copy\n  FIELDS text\n  SEARCH ANALYZER ocr_ascii_lower  BM25; 
REBUILD INDEX ocr_text_ft ON ocr_copy;
SELECT id, search::score()\nFROM ocr_copy\nWHERE text @@ "Jacen Wong";
INFO FOR TABLE ocr_copy;\n
SELECT\n  id,\n  search::score() AS score,                      -- BM25 relevance\n  search::highlight(text, '→', '←') AS snippet   -- optional\nFROM ocr_copy\nWHERE text @@ "invoice number 1234"\nORDER BY score DESC\nLIMIT 20;\n
SELECT\n  id,\n  search::score() AS score,                      -- BM25 relevance\n  search::highlight(text, '→', '←') AS snippet   -- optional\nFROM ocr_copy\nWHERE text @@ "Jacen Wong"\nORDER BY score DESC\nLIMIT 20;\n
SELECT\n  id,\n  search::score() AS score,                      -- BM25 relevance\n  search::highlight(text, '→', '←') AS snippet   -- optional\nFROM ocr_copy\nWHERE text @@ "Jacen"\nORDER BY score DESC\nLIMIT 20;\n
SELECT\n  id,\n  search::score() AS score,                      -- BM25 relevance\n  search::highlight(text, '→', '←') AS snippet   -- optional\nFROM ocr_copy\nWHERE text @@ "lifelog"\nORDER BY score DESC\nLIMIT 20;\n
SELECT\n  id,\n  search::score(0)                    AS score,      -- BM25 relevance\n  search::highlight('→','←',0)        AS snippet     -- wrap hits\nFROM ocr_copy\nWHERE text @0@ "lifelog"                              -- ← give the clause its 0-label\nORDER BY score DESC\nLIMIT 20;\n
SELECT\n  id,\n  search::score(0)                    AS score,      -- BM25 relevance\n  search::highlight('→','←',0)        AS snippet     -- wrap hits\nFROM ocr_copy\nWHERE text @0@ "Jacen Wong"                              -- ← give the clause its 0-label\nORDER BY score DESC\nLIMIT 20;\n
SELECT\n  id,\n  search::score(0)                    AS score\nFROM ocr_copy\nWHERE text @0@ "Jacen Wong"                              -- ← give the clause its 0-label\nORDER BY score DESC\nLIMIT 20;\n
select count() from ocr_copy
select count() from ocr_copy group all
SELECT\n  id,\n  search::score(0)                    AS score\nFROM ocr_copy\nWHERE text @0@ "Jacen Wong"                              -- ← give the clause its 0-label\nORDER BY score DESC\nLIMIT 20;\n
SELECT\n  id,\n  search::score(0)                    AS score\nFROM ocr_copy\nWHERE text @0@ "Islam"                              -- ← give the clause its 0-label\nORDER BY score DESC\nLIMIT 20;\n
select * from ocr_text limit 1;
select * from ocr_text_copy limit 1;
select * from ocr_copy limit 1;
select uuid from ocr_copy limit 1;
select record::id(id) from ocr_copy limit 1;
SELECT count() AS n FROM upload_chunks GROUP ALL;
SELECT collector_id,stream_id,offset,created_at FROM upload_chunks ORDER BY created_at DESC LIMIT 10;
SELECT count() AS n FROM `implement_this:Processes` GROUP ALL;
SELECT timestamp, process_name, pid FROM `implement_this:Processes` ORDER BY timestamp DESC LIMIT 5;
SELECT count() AS n FROM upload_chunks GROUP ALL;
SELECT collector_id,stream_id,offset,created_at FROM upload_chunks ORDER BY created_at DESC LIMIT 10;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id;
SELECT collector_id,stream_id,offset,created_at FROM upload_chunks ORDER BY created_at DESC LIMIT 8;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id ORDER BY stream_id;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id ORDER BY stream_id;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY  stream_id ORDER BY stream_id;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id ORDER BY stream_id;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id;
SELECT * FROM upload_chunks WHERE stream_id = "processes" LIMIT 3;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT stream_id, count() AS n FROM upload_chunks GROUP BY stream_id;
SELECT origin, count() AS n FROM data GROUP BY origin;
SELECT * FROM catalog LIMIT 20;
SELECT collector_id, stream_id, count() AS n FROM upload_chunks GROUP BY collector_id, stream_id;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT collector_id, stream_id, offset, indexed FROM upload_chunks WHERE stream_id = "screen" ORDER BY offset DESC LIMIT 5;
SELECT collector_id, stream_id, offset, indexed FROM upload_chunks WHERE stream_id = "screen" ORDER BY offset DESC LIMIT 1;
SELECT offset, indexed FROM upload_chunks WHERE stream_id = "screen" ORDER BY offset DESC LIMIT 10;
SELECT offset, indexed FROM upload_chunks WHERE stream_id = "screen" ORDER BY offset DESC LIMIT 5;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT count() AS n FROM `laptop:Screen`;
SELECT * FROM `laptop:Screen` ORDER BY timestamp DESC LIMIT 1;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
SELECT offset, indexed FROM upload_chunks WHERE stream_id = "screen" ORDER BY offset DESC LIMIT 10;
SELECT count() AS n FROM upload_chunks WHERE stream_id = 'processes' GROUP ALL;
