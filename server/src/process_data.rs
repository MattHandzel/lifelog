
pub fn process_data(table){

    // Look up the table in the data base and the config
    // Look at what data processes the user wants for this specific data
    // Query the table for unprocessed data
    //

}
